# DemoTyper 项目说明

## 项目简介

DemoTyper 是一款专为演示与教学场景设计的 VS Code / Cursor 扩展，帮助演讲者、讲师或开发者在展示时“伪装”实时写代码，实际则是让扩展根据预先保存的目标文件逐步恢复内容，确保脚本顺序一致且可控。

## 核心功能

### 秘密模式（Secret Mode）

当前版本仅保留 **智能替换模式（Smart Replace）**：
- 打开一个空白或任意编辑器，将其设置为目标文件后即可进入秘密模式；
- 敲击任意按键时，扩展会按照目标文件的差异分析结果自动插入下一个正确字符；
- 光标移动、跳转下一个差异等行为均由扩展控制，保证演示严格匹配原稿；
- 如果缓冲区与快照不同步，会立即发出警告并退出秘密模式，避免脚本错乱。

### 目标文件快照

- **设置目标文件**：在资源管理器中右键文件，选择 “Set as Demo Target File”。扩展会立刻读取当前内容，存储在 `globalStorage/targetSnapshot.json` 中。
- **清除目标**：通过命令面板或侧边栏清除当前文件快照，或一次性清除全部快照。
- **恢复当前文件**：读取快照并覆盖当前编辑器内容。
- **跳转差异**：快速定位到尚未“补写”的位置。

## 工作原理

1. **extension.ts**
   - 注册所有命令、视图和事件监听器；
   - 持有 `SecretModeHandler`、`SmartReplaceHandler`、`TargetFileManager` 等核心对象。

2. **secretModeHandler.ts**
   - 拦截 `type` / `deleteLeft` 命令；
   - 串行化输入操作，并在缺乏目标文件时阻止启动或自动退出秘密模式；
   - 与 `SmartReplaceHandler` 协作，把键盘事件转译为目标文件的差异补全。

3. **smartReplaceHandler.ts**
   - 基于子序列扫描算法计算“下一个缺失字符”；
   - 将文本插入到正确的位置，并保持光标同步；
   - 支持 `Jump to Next Gap` 功能。

4. **targetFileManager.ts**
   - 管理 `targetSnapshot.json` 中的所有快照；
   - 提供设置、清除、读取和查询目标文件的 API；
   - 缓存快照以减少文件系统访问。

5. **statusBarManager.ts / sidebarProvider.ts**
   - 状态栏在秘密模式中显示 “Secret (Smart · <label>)”，非秘密模式显示 “Ready”；
   - 侧边栏列出所有保存的目标文件，并附带操作按钮（设置、清除、恢复、跳转差异等）。

6. **notificationManager.ts / logger.ts**
   - 统一处理提示、警告及错误；
   - 在启用日志时输出关键操作。

## 使用流程

1. 打开需要演示的文件，右键选择 “Set as Demo Target File”。
2. 在空白缓冲区或任意编辑器中按 `Ctrl + Shift + Alt + S`（macOS：`Cmd + Shift + Alt + S`）进入秘密模式。
3. 按任意键，Smart Replace 自动插入目标文件的下一个字符。
4. 按命令面板中的 “Jump to Next Gap” 可直接跳到下一处差异。
5. 演示完成再次触发切换命令即可退出秘密模式。

> **注意**：若当前文件没有快照，秘密模式不会启动；如果在秘密模式中切换到无快照的文件，扩展会自动退出并提醒用户。

## 配置项

- `demotyper.enableLogging`：是否在输出面板记录调试信息。
- `demotyper.notifications.muteGuidance`：是否静音扩展弹出的提示与警告。

## 快捷键与命令

- `demotyper.toggleSecretMode`：切换秘密模式（默认快捷键 `Ctrl/Cmd + Shift + Alt + S`）。
- `demotyper.setAsTargetFile`：将当前文件保存为演示快照。
- `demotyper.clearTargetFile`：移除当前文件对应的快照。
- `demotyper.clearAllTargets`：清除所有快照。
- `demotyper.restoreCurrentFile`：将编辑器内容恢复为快照。
- `demotyper.jumpToNextGap`：跳转到下一处需要补写的位置。

## 适用场景

- **会议/大会演讲**：提前准备脚本，现场“实时”写代码。
- **课堂教学**：保证多次授课的内容一致且可控。
- **产品演示**：在不同客户环境中快速重现固定脚本。
- **录屏教程**：录制时集中讲解思路，避免频繁停顿。

## 开发与打包

```bash
npm install
npm run compile
# VS Code 中使用 F5 进入 Extension Development Host
# 或使用 npx vsce package 打包 VSIX
```

生成的 VSIX 文件可直接在 VS Code / Cursor 中安装。

## 项目结构

```
DemoTyper/
├── src/
│   ├── extension.ts
│   ├── config.ts
│   ├── secretModeHandler.ts
│   ├── smartReplaceHandler.ts
│   ├── targetFileManager.ts
│   ├── statusBarManager.ts
│   ├── sidebarProvider.ts
│   ├── notificationManager.ts
│   └── logger.ts
├── docs/
│   ├── multi-file-secret-mode.md
│   └── target-snapshot.md
├── package.json
├── tsconfig.json
└── README.md
```

## 后续计划

- 支持更多辅助工具（例如自动恢复到特定步骤、脚本预览等）。
- 在侧边栏中显示差异统计或进度指示。
- 增强错误提示与自检工具，避免快照损坏导致演示中断。

## 结语

DemoTyper 通过“秘密模式 + 目标快照”的组合，为演讲者和讲师提供了稳定、可预期的演示体验。只需提前准备好脚本，就能在舞台上自如地“敲键盘”，把精力集中在讲解思路与亮点上。
